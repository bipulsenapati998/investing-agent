Flask==2.3.3
Flask-CORS==4.0.0
python-dotenv==1.0.0
Werkzeug==2.3.7

# Core AutoGen libraries
autogen-core
autogen-agentchat
autogen-ext[openai]

# Environment management
python-dotenv

# Async support (usually included with Python 3.7+)
asyncio